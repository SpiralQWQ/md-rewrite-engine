# 执行者选择器 + 本体参与重排 · 施工 Task 清单（fixloop S1 穷举闭包）

> **依据**：`docs/executor-selector-plan.md`（6 阶段计划）+ `~/.claude/spec/_exhaustive_test_charter.md`（fixloop 规范）
> **执行规则**：逐 Task → 4 轮审核（完成度/回归影响/隐蔽缺陷/质量设计）→ 全过才进下一个 → 证据单写 `temp/fixloop_evidence/round_N.md`
> **顺序**：阶段 1 → 6
> **共 27 个 Task + 穷举环节（S2）+ 最终验收（S4）+ CHANGELOG**
> **现状**：阶段 1 代码已部分落地（executors.yaml + llm_client 路由 + 测试半改），Task-01 从"补完+验证"开始

---

## 阶段 1 · 执行者选择器（让 4 环节可配执行者）

| Task | 内容（最小可验证单元） | 验收点 | 涉及文件 |
|---|---|---|---|
| Task-01 | 补完 test_llm_client 适配 executors（rewrite=claude→anthropic，quality=glm）+ 全量回归 | executors 路由测试全绿；回归无破坏 | `tests/providers/test_llm_client.py` |
| Task-02 | 执行者选择交互：跑之前对话询问 4 环节 → 更新 executors.yaml | 对话问 4 环节、回答后配置生效 | `cli.py` 或交互层 |
| Task-03 | 阶段 1 验证：路由按 executors.yaml 走（rewrite→anthropic/claude，quality→glm） | 4 环节路由与 executors 一致 | 验证脚本 |

## 阶段 2 · 本体参与骨架（B 方向：我主控，脚本=工具库）

| Task | 内容（最小可验证单元） | 验收点 | 涉及文件 |
|---|---|---|---|
| Task-04 | 能力函数清单设计：我主控要单环节调用的函数（物理分块兜底/概念地图/GLM质检/评分/机械对账/输出外置）+ 数据结构 | 函数清单完整、接口明确 | `docs/` 设计 |
| Task-05 | 实现能力函数暴露：orchestrator 拆出可单环节调用的函数 | 每个环节函数可独立调用 | `services/orchestrator.py` |
| Task-06 | 我主控编排流程：我按什么顺序调这些函数完成整条流水线 | 编排流程可执行、上下文连贯 | `docs/` 编排手册 |
| Task-07 | 阶段 2 验证：模拟"我主控"完整跑一遍（我读转写→重排→调脚本质检→复核→输出） | 流程跑通、无割裂 | 验证脚本 |

## 阶段 3 · 本体主导全局处理

| Task | 内容 | 验收点 | 涉及文件 |
|---|---|---|---|
| Task-08 | 概念地图先行：重排前本体读全文抽核心概念地图 | 长转写先出概念地图 | `services/orchestrator.py` |
| Task-09 | 语义分块：本体定分块 + 机器超长兜底 | 分块由本体主导、机器保底 | `core/chunk.py`、`services/` |
| Task-10 | 逐块重排带上下文：每块带地图+前文摘要+后文预告+重叠 | 块间不隔离、前后连贯 | `core/rewrite.py`、`services/llm.py` |
| Task-11 | 整体审一遍：所有块做完通读检查连贯/重复/遗漏/归位 | 长转写产出无重复遗漏 | `services/orchestrator.py` |
| Task-12 | 阶段 3 验证 | 长转写产出前后连贯 | 验证脚本 |

## 阶段 4 · 提示词家族（详细度/结构/检查写死）

| Task | 内容 | 验收点 | 涉及文件 |
|---|---|---|---|
| Task-13 | P1 本体重排提示词（详细展开/少删/口语可丢/事实保留/6件套/费曼4角度/schema结构） | 提示词含全部详细度规则 | `configs/` 或 `services/llm.py` |
| Task-14 | P2 本体自查提示词（AI 教学视角重读挑毛病） | 自查维度齐全、灵活不僵化 | 同上 |
| Task-15 | P3 本体对账提示词（对照概念地图查漏/概念偷换） | 对账依据概念地图 | 同上 |
| Task-16 | P4 GLM 质检提示词（AI友好/详细/准确/完整 四组维度） | 4 组维度完整 | `services/llm.py` |
| Task-17 | P5 GLM 评分提示词（95 目标线，具体问题主导） | 评分+问题结合 | 同上 |
| Task-18 | 阶段 4 验证 | 产出 6件套/费曼4角度/事实保留 | 验证 |

## 阶段 5 · 验证打回闭环

| Task | 内容 | 验收点 | 涉及文件 |
|---|---|---|---|
| Task-19 | fail-closed 门禁：P4/P5 不合规 → 挂起/修补而非直接过 | 不达标物理进不了"已通过" | `services/orchestrator.py` |
| Task-20 | 每块独立 4 次修补额度；第 4 次仍不过 → 该块挂起等人工 | 各块互不挤占、到限挂起 | 同上 |
| Task-21 | 原文保护 + 机械对账兜底（关键点覆盖率≥90%） | 修补不删关键内容 | `core/verify.py` |
| Task-22 | 反馈累积（带问题清单回 P1 针对性修补） | 打回=带清单修补非重做 | `services/` |
| Task-23 | 阶段 5 验证 | 能打回、能挂起、不死循环、不修没 | 验证 |

## 阶段 6 · 收尾

| Task | 内容 | 验收点 | 涉及文件 |
|---|---|---|---|
| Task-24 | 完整转写附录外置到源文件（不干扰 AI 教学上下文） | 笔记不含附录、原文存源文件 | `services/` |
| Task-25 | 输出笔记（汇总各块 + 整体审结果） | 完整笔记落盘 | `services/orchestrator.py` |
| Task-26 | 使用手册（怎么跑/配执行者/换执行者） | 手册可照着跑 | `docs/` |
| Task-27 | 阶段 6 验证 + 全量回归 | 真实转写跑通、回归全绿 | 验证 |

---

## 穷举环节（S2）— 全部 Task 完成后
三层穷举（路径/边界/终点一致性）：executors 路由各组合、插槽"等本体/超时/中断"、分块上下文、打回循环边界、附录外置、CLI 交互防呆。收敛判定 high=0。

## 最终验收（S4）
- 全量回归 + 终点一致性分组比对
- 证据单核查（round_N 齐全、含现象/根因/核心修复/影响面）
- 验收报告 `docs/acceptance-report-v0.3.0.md`（对齐版本）
- CHANGELOG 追加 0.3.0 段（Keep a Changelog）
